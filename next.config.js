/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  images: {
    domains: ["i.ibb.co",'images.unsplash.com', "scontent-lga3-2.cdninstagram.com"],
  },
};

module.exports = nextConfig;
